{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Lancer le backend (avec .env)",
      "type": "go",
      "request": "launch",
      "mode": "auto",
      "program": "${workspaceFolder}/backend",
      "cwd": "${workspaceFolder}/backend",
      "envFile": "${workspaceFolder}/.env",
      "preLaunchTask": "4. Build complet (frontend + copie)"
    }
  ]
}